import { DefaultTheme } from 'styled-components';

const colors = {
  primary: '#6cc1ba',
  white: '#ffffff',
  black: '#000000',
};

const fontSize = {
  title: 24,
  subTitle: 20,
  content: 18,
};

const theme: DefaultTheme = {
  colors,
  fontSize,
};

export default theme;
